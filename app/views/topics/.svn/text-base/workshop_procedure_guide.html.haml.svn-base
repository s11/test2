- cache([resource, "workshop-procedure-guide"]) do
  #topic
    = render :partial => "navigation"

    %iframe{ :frameborder => '0', :align => 'left', :width => '100%', :height => '800px', :src => "#{controller.relative_url_root}/assets/WS/#{resource.workshop_procedure_guide_filename}" }
