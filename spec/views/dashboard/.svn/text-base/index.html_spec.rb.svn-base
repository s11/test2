require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe "/dashboard/index.html" do
  subject do
    render 'dashboard/index.html'
    response
  end

  it "should render" do
    should be
  end
end
