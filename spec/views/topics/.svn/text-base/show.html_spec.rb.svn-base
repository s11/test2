require File.expand_path(File.dirname(__FILE__) + '/../../spec_helper')

describe "/topics/show.html" do
  subject do
    render 'topics/show.html'
    response
  end

  it "should render" do
    should be
  end
end
